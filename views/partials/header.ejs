<header class="fixed top-0 left-0 right-0 z-50 flex flex-col lg:flex-row items-center divide-x-0 lg:divide-x-2 p-2 bg-gray-800 text-white shadow-md">
  <!-- Title (Responsive 100% for small screens, 15% for large screens) -->
  <div class="w-full lg:w-60 lg:pl-8 mb-2 lg:mb-0">
    <h1 class="text-2xl font-bold ml-8">Evan Store</h1>
  </div>

  <!-- Search Bar (Responsive 100% for small screens, 85% for large screens) -->
  <div class="w-full lg:flex-1 pl-0 lg:pl-4">
    <input
      type="text"
      placeholder="Search..."
      class="w-full p-2 rounded bg-gray-600 text-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500"
    />
  </div>
</header>